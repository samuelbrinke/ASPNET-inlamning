@page
@model ASPNET_inlamning.Pages.MyEvents.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
@*    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Place)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].SpotsAvalible)
            </th>
            <th></th>
        </tr>
    </thead>*@
@foreach (var item in Model.AttendeeEvent) {
        <div>
            <p>
                @Html.DisplayFor(modelItem => item.Event.Title)
            </p>
            <p>
                @Html.DisplayFor(modelItem => item.Event.Description)
            </p>
            <p>
                @Html.DisplayFor(modelItem => item.Event.Place)
            </p>
            <p>
                @Html.DisplayFor(modelItem => item.Event.Address)
            </p>
            <p>
                @Html.DisplayFor(modelItem => item.Event.Date)
            </p>
            <p>
                @Html.DisplayFor(modelItem => item.Event.SpotsAvalible)
            </p>
            <p>
                <a asp-page="./Edit" asp-route-id="@item.EventID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EventID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.EventID">Delete</a>
            </p>
        </div>
}
</table>
